#include "main.h"

/**
 * _isdigit - function
 *
 * print lphabet_x10 - Prints the alphabet in lowercase followed by a newline
 * @c: charactere
 * Return: Nothing (void)
*/
void print_numbers(void)
{
    char n;
    for ( n = '0' ; n <= '9' ; n++)
    
    _putchar(n);
    _putchar('\n');
    _putchar('\n');
}