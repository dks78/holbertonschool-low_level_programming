#include "main.h"

/**
 * _isdigit - function
 *
 * print_numbers: - Prints the alphabet in lowercase followed by a newline
 * n: charactere
 * Return: Nothing (void)
*/
void print_numbers(void)
{

char n;

for (n = '0' ; n <= '9' ; n++)

_putchar(n);
_putchar('\n');
}
